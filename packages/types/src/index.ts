/// <reference path="../types/index.d.ts" />

export enum ServerActions {
  Install = "install",
  Start = "start",
  Stop = "stop",
  Command = "command"
}

export enum ServerCoreType {
  Forge = "forge",
  Fabric = "fabric"
}
